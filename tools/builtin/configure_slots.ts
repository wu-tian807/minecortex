import { makeCapabilityTool } from "./_capability-utils.js";

export default makeCapabilityTool({
  name: "configure_slots",
  kind: "slots",
  subject: "context slots injected into the brain's system prompt",
});
